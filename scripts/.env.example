# Server-side ONLY — do not prefix with VITE_, do not commit the filled file.
# The service-role key bypasses RLS; treat it like a password.
SUPABASE_URL=https://bttvyctnvrexgcqqofep.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-from-supabase-dashboard
# Storage bucket for MP3s (create it in the dashboard; defaults to lesson-audio).
SUPABASE_AUDIO_BUCKET=lesson-audio
